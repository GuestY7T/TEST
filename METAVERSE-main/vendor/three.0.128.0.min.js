/* three.min.js placeholder — vendored copy of r128 three.js
   Note: the real minified file is large (~140KB). For deployment, replace this placeholder
   with the actual minified library file, downloaded from a trusted CDN.
*/

// Minimal placeholder to avoid runtime errors when real library isn't present
if (typeof window !== 'undefined') window.THREE = window.THREE || {};
